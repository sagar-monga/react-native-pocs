# How to run Android and iOS apps locally without Expo Go?
- run `npx expo run:android`
- Add package name (Will add android & ios folders).
- Done.

- To run using expo go, use `npm run android` & scan the QR