export enum AppRoutes {
  AuthenticatedRoutes = 'Authenticated',
  UnauthenticatedRoutes = 'Unauthenticated',
}

export enum AuthenticatedRoutes {
  Home = 'Home',
  FormInput = 'FormInput',
  NewFormInput = 'NewFormInput',
  TailwindStyles = 'TailwindStylesFormInput',
}

export enum UnauthenticatedRoutes {
  Login = 'Login',
}