# FIREBASE SOLUTION FOR DEVELOPER ERROR

Use the command `cd android && ./gradlew signingReport` at project root.
Use the SHA key generated which is variant - debug, config - debug and store location is in the app